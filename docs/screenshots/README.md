# Live submission screenshots

After the owner verifies the real backend and Streamlit flow, capture:

- `1_multi_turn_inventory.png`: inventory results, a positional follow-up and warranty response.
- `2_returning_user_new_session.png`: the same user in a new session recalling saved preferences.

Do not include credentials or sensitive information. Do not substitute simulated screenshots
for live verification. This directory intentionally contains no fabricated demo images.
